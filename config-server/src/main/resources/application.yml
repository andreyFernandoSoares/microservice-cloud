
server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/andreyFernandoSoares/microserver-repo
          search-paths: /fornecedor, /loja, /transportador
          